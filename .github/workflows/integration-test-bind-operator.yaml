name: Bind-operator integration tests

on:
  pull_request:
    paths:
      - 'bind-operator/charmcraft.yaml'
      - 'bind-operator/lib/**'
      - 'bind-operator/pyproject.toml'
      - 'bind-operator/requirements.txt'
      - 'bind-operator/src/**'
      - 'bind-operator/tests/**'
      - 'bind-operator/tox.ini'
      - '.github/workflows/integration-test-bind-operator.yaml'
  push:
    branches:
      - main

jobs:
  integration-tests:
    uses: canonical/operator-workflows/.github/workflows/integration_test.yaml@main
    secrets: inherit
    with:
      self-hosted-runner: false
      self-hosted-runner-label: "edge"
      juju-channel: 4.0/stable
      provider: lxd
      working-directory: ./bind-operator
