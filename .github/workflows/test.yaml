name: Tests

on:
  pull_request:

jobs:
  unit-tests-bind-operator:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
    with:
      self-hosted-runner: false
      working-directory: ./bind-operator
