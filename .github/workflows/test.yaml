name: Tests

on:
  pull_request:

jobs:
  unit-tests-bind-operator:
    uses: nrobinaubertin/stable-operator-workflows/.github/workflows/test.yaml@main
    secrets: inherit
    with:
      self-hosted-runner: false
      self-hosted-runner-label: "edge"
      working-directory: ./bind-operator
